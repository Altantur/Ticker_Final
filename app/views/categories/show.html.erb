<!-- Title -->
<% title "Зөвхөн хичээлийн бие даалт" %>

<% @posts.each do |post| %>

	<div class="panel mypanel-category">
    	<div class="panel-heading"><%= link_to post.title , "/news/" + post.id.to_s %></div>
    	<%= link_to image_tag("news.jpg", :alt => post.title, :title => post.title, :style =>"width:100%" ), "/news/" + post.id.to_s %>

    	<div class="panel-body"><%=post.body %></div>
    	<div class="panel-footer">Огноо: <%=post.created_at %> | Коментийн тоо: <fb:comments-count href="<%= "http://www.tngri.com/" + "/news/" + post.id.to_s %> "</fb:comments-count></div>
  </div>

<%end%>

<div style="text-align:center">
<%= will_paginate @posts, renderer: BootstrapPagination::Rails, previous_label: "Өмнөх", next_label: "Дараах" %>
</div>