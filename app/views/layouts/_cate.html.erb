<div class="container">
    <div class="row">

      <% @pro.each do |product| %>
        <p>Бүтээгдэхүүний нэр: <%= product.name %></p>
        <p>Үнэлгээ: <%= product.value %></p>
      <% end %>
      	
      	
	</div>
	<div class="row">
	<p>Таны үнэлгээ: </p>
	</div>
  <p>Боломжит газрууд болон үнэ:</p>
  <% @loc.each do |location| %>
<p>
  <%= location.address + ":" + location.price.to_s + "₮" %>
</p>
  <% end %>
  
</div>