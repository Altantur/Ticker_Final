	<% title @page.title %>
	<div class="panel mypanel-category">
	<div class="panel-heading"><%= link_to @page.title , "/news/" + @page.id.to_s %></div>
    	<%= link_to image_tag(@page.image_path_url, :alt => @page.title, :title => @page.title, :style =>"width:100%" ), "/news/" + @page.id.to_s %>
    	<div class="panel-body"><%=@page.content %></div>
    	<div class="panel-footer">Бичсэн: <%=@page.created_at.strftime("%Y/%m/%d-ний %H:%M") %></div>
  </div>
